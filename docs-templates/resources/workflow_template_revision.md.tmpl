---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "stackguardian_workflow_template_revision Resource - terraform-provider-stackguardian"
subcategory: "Templates"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}

---

# stackguardian_workflow_template_revision (Resource)

{{ .Description | trimspace }}

## Revisions

Each revision is a version of its parent
[`stackguardian_workflow_template`](https://registry.terraform.io/providers/StackGuardian/stackguardian/latest/docs/resources/workflow_template), referenced by `template_id`. Create a new
revision to change what workflows inherit, rather than editing an existing one.

- `is_public` publishes the revision, making it available to reference from
  [`stackguardian_workflow_from_template`](https://registry.terraform.io/providers/StackGuardian/stackguardian/latest/docs/resources/workflow_from_template).
- `deprecation` steers users off an old revision; `deprecation.message` should name the
  replacement.

## What workflows inherit

Every attribute set here becomes the default for workflows created from this revision. A workflow
that declares the same attribute overrides it; one that omits it inherits whatever this revision
sets — including after a revision upgrade.

## Example Usage

{{tffile "docs-examples/resources/workflow_template_revision/resource.tf"}}

{{ .SchemaMarkdown }}

## Import

Import existing resource

### Using Import block (terraform v1.5.0 and later)
```terraform
import {
  to = stackguardian_workflow_template_revision.example
  id = "template-name:revision-version"
}
```

### Using CLI
```bash
terraform import stackguardian_workflow_template_revision.example template-name:revision-version
```

## Building this with AI

<!-- AI-SKILLS:START -->
Generating `stackguardian_workflow_template_revision` configuration with an AI assistant? Load the **`stackguardian-templates`** skill, which covers this resource's arguments and the mistakes it invites.

**Worth knowing either way:** Revision numbers are assigned by the platform, not chosen. The matching **data source** accepts only the bare `<name>:<revision>` form — the organization-qualified path returns `Unauthorized`, not a not-found.

The skills live in [the provider repository](https://github.com/StackGuardian/terraform-provider-stackguardian/tree/main/.claude/skills) and work with Claude Code, Cursor, Copilot, Windsurf and any agent that reads [`AGENTS.md`](https://github.com/StackGuardian/terraform-provider-stackguardian/blob/main/AGENTS.md).
<!-- AI-SKILLS:END -->
