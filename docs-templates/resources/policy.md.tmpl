---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "stackguardian_policy Resource - terraform-provider-stackguardian"
subcategory: "Access Control"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}

---

# stackguardian_policy (Resource)

{{ .Description | trimspace }}

## Scoping a policy

`enforced_on` takes resource paths, so a policy can apply to a whole workflow group or to a
single workflow. Those are the same paths used by a `stackguardian_role`'s
`allowed_permissions`, so a policy can be scoped to exactly the part of the organization a team
already owns.

## Where the policy body comes from

Supply the body one of two ways:

- `policy_input_data` — the policy written inline.
- `policy_vcs_config` — pulled from version control, or from the marketplace by setting
  `use_marketplace_template` and `policy_template_id`.

## Example Usage

{{tffile "docs-examples/resources/policy/resource.tf"}}

{{ .SchemaMarkdown }}

## Import

Import existing resource

### Using Import block (terraform v1.5.0 and later)
```terraform
import {
  to = stackguardian_policy.example-policy
  id = "policy-name"
}
```

### Using CLI
```bash
terraform import stackguardian_policy.example-policy policy-name
```